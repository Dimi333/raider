import { FilterByGroupPipe } from './filter-by-group.pipe';

describe('FilterByPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByGroupPipe();
    expect(pipe).toBeTruthy();
  });
});
